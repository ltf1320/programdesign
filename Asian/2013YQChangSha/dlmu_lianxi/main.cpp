#include <iostream>
#include <cstdio>
#include <cstring>
#include <algorithm>
using namespace std;
const int maxn=1111;
char ch[maxn];
int main()
{
    while(gets(ch))
    {
        int len=strlen(ch);
        if(len%2)
        {
            int p=len/2;
            for(int i=0;i<p;i++)
        }
    }
    return 0;
}
